[project]
name = "wechat-article"
version = "1.0.0"
description = "Convert WeChat Official Account articles to Markdown"
requires-python = ">=3.10"
dependencies = [
    "camoufox[geoip]",
    "markdownify",
    "beautifulsoup4",
    "httpx",
]

[tool.uv]
dev-dependencies = []
